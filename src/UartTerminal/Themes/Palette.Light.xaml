<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================================
         UartTerminal — 라이트 팔레트 (GitHub-light 계열).
         Palette.Dark.xaml 과 <b>키 집합이 완전히 같아야</b> 한다(Theme.Apply 가 검사한다).

         다크를 그대로 반전하면 안 된다 — 흰 배경에서는
          · 채도 높은 색(노랑/시안)이 거의 안 보여 더 어둡게 눌러야 하고,
          · 경계선은 '밝은 회색' 이 아니라 '어두운 회색' 이어야 3:1 이 나온다.
         모든 값은 theme-audit.ps1 로 대비를 측정해 맞췄다.
         ============================================================ -->

    <!-- ===== Color palette ===== -->
    <Color x:Key="C.Bg">#FFFFFFFF</Color>
    <Color x:Key="C.BgDeep">#FFEFF2F6</Color>
    <Color x:Key="C.BgPanel">#FFF6F8FA</Color>
    <Color x:Key="C.Surface">#FFFFFFFF</Color>
    <Color x:Key="C.SurfaceHover">#FFEAEEF2</Color>

    <!-- 경계 3종(다크와 같은 역할): 장식 / 컨트롤 윤곽(3:1 필수) / 강조 -->
    <Color x:Key="C.Border">#FFB6BFCA</Color>
    <Color x:Key="C.BorderStrong">#FF737E8C</Color>
    <Color x:Key="C.BorderFocus">#FF4C5561</Color>
    <Color x:Key="C.TabIdle">#FFEDF1F5</Color>
    <Color x:Key="C.Text">#FF1F2328</Color>
    <Color x:Key="C.TextDim">#FF41474E</Color>
    <Color x:Key="C.TextMuted">#FF57606A</Color>
    <Color x:Key="C.TextFaint">#FF636C77</Color>
    <Color x:Key="C.Accent">#FF0969DA</Color>
    <Color x:Key="C.AccentDeep">#FF0550AE</Color>
    <!-- 상태색은 흰 배경 기준으로 눌러 4.5:1 이상 -->
    <Color x:Key="C.Green">#FF15702F</Color>
    <Color x:Key="C.GreenBright">#FF116329</Color>
    <Color x:Key="C.Blue">#FF0550AE</Color>
    <Color x:Key="C.Amber">#FF7D4E00</Color>
    <Color x:Key="C.Red">#FFCF222E</Color>
    <Color x:Key="C.Purple">#FF6639BA</Color>
    <Color x:Key="C.State">#FFBC4C00</Color>
    <Color x:Key="C.Idle">#FF7D8590</Color>

    <!-- ===== Brushes ===== -->
    <SolidColorBrush x:Key="Bg"            Color="{StaticResource C.Bg}"/>
    <SolidColorBrush x:Key="BgDeep"        Color="{StaticResource C.BgDeep}"/>
    <SolidColorBrush x:Key="BgPanel"       Color="{StaticResource C.BgPanel}"/>
    <SolidColorBrush x:Key="Surface"       Color="{StaticResource C.Surface}"/>
    <SolidColorBrush x:Key="SurfaceHover"  Color="{StaticResource C.SurfaceHover}"/>
    <SolidColorBrush x:Key="BorderBrush2"  Color="{StaticResource C.Border}"/>
    <SolidColorBrush x:Key="BorderStrong"  Color="{StaticResource C.BorderStrong}"/>
    <SolidColorBrush x:Key="BorderFocus"   Color="{StaticResource C.BorderFocus}"/>
    <SolidColorBrush x:Key="TabIdle"       Color="{StaticResource C.TabIdle}"/>
    <SolidColorBrush x:Key="Text"          Color="{StaticResource C.Text}"/>
    <SolidColorBrush x:Key="TextDim"       Color="{StaticResource C.TextDim}"/>
    <SolidColorBrush x:Key="TextMuted"     Color="{StaticResource C.TextMuted}"/>
    <SolidColorBrush x:Key="TextFaint"     Color="{StaticResource C.TextFaint}"/>
    <SolidColorBrush x:Key="Accent"        Color="{StaticResource C.Accent}"/>
    <SolidColorBrush x:Key="AccentDeep"    Color="{StaticResource C.AccentDeep}"/>
    <SolidColorBrush x:Key="Green"         Color="{StaticResource C.Green}"/>
    <SolidColorBrush x:Key="GreenBright"   Color="{StaticResource C.GreenBright}"/>
    <SolidColorBrush x:Key="Blue"          Color="{StaticResource C.Blue}"/>
    <SolidColorBrush x:Key="Amber"         Color="{StaticResource C.Amber}"/>
    <SolidColorBrush x:Key="Red"           Color="{StaticResource C.Red}"/>
    <SolidColorBrush x:Key="Purple"        Color="{StaticResource C.Purple}"/>
    <SolidColorBrush x:Key="StateBrush"    Color="{StaticResource C.State}"/>
    <SolidColorBrush x:Key="Idle"          Color="{StaticResource C.Idle}"/>

    <!-- 스크롤 손잡이(3:1 이상) -->
    <SolidColorBrush x:Key="ScrollThumb"      Color="#FF7C8794"/>
    <SolidColorBrush x:Key="ScrollThumbHover" Color="#FF57606A"/>

    <!-- ===== 코드에서 쓰는 브러시 ===== -->
    <SolidColorBrush x:Key="PanelBorderInactive" Color="{StaticResource C.Border}"/>
    <SolidColorBrush x:Key="PanelHeaderBg"       Color="{StaticResource C.TabIdle}"/>
    <SolidColorBrush x:Key="ContentBg"           Color="{StaticResource C.Bg}"/>
    <SolidColorBrush x:Key="SplitterBrush"       Color="{StaticResource C.Border}"/>
    <SolidColorBrush x:Key="TitleActiveFg"       Color="{StaticResource C.Text}"/>
    <SolidColorBrush x:Key="TitleInactiveFg"     Color="{StaticResource C.TextMuted}"/>
    <SolidColorBrush x:Key="DotConnected"        Color="{StaticResource C.GreenBright}"/>
    <SolidColorBrush x:Key="DotIdle"             Color="{StaticResource C.Idle}"/>
    <SolidColorBrush x:Key="DotReconnecting"     Color="{StaticResource C.Amber}"/>
    <SolidColorBrush x:Key="DotReleased"         Color="{StaticResource C.Purple}"/>
    <SolidColorBrush x:Key="PromptActive"        Color="{StaticResource C.Green}"/>
    <SolidColorBrush x:Key="PromptIdle"          Color="{StaticResource C.GreenBright}"/>

    <!-- 오버레이(찾기 바·줌 표시기) 배경 — 밝은 패널 + 거의 불투명 -->
    <SolidColorBrush x:Key="OverlayBg" Color="#F5F6F8FA"/>

    <!-- 터미널 렌더러: 흰 배경 + 어두운 글자. 선택/검색 하이라이트도 밝은 배경용으로 바꾼다 -->
    <Color x:Key="C.TermFg">#FF1F2328</Color>
    <Color x:Key="C.TermBg">#FFFFFFFF</Color>
    <Color x:Key="C.TermCursor">#FF1A7F37</Color>
    <!-- 선택 영역은 글자 위에 겹치므로 반투명(알파) — 불투명이면 글자를 덮는다 -->
    <Color x:Key="C.TermSelection">#590969DA</Color>
    <Color x:Key="C.TermTimestamp">#FF6B747E</Color>
    <Color x:Key="C.TermSearchMatch">#FFFFF1A8</Color>
    <Color x:Key="C.TermSearchCurrent">#FFFFC93C</Color>
    <Color x:Key="C.TermMeta">#FF6639BA</Color>

    <!-- ===== ANSI 16색 (라이트) =====
         다크 값을 그대로 쓰면 흰 배경에서 초록 2.16:1 · 노랑 1.70:1 · bright white 1.00:1 로
         ESP-IDF 로그가 안 읽힌다. 채도 높은 색을 눌러 전부 4.5:1 이상으로 맞췄다
         (측정: scratchpad/pick-ansi.py — 16/16 통과, 최저 red 5.36:1).
         bright(8~15)는 흰 배경에서 '더 밝게' 가 아니라 '더 진하게' 가 두드러진다
         (예외: bright black 은 원래 흐린 회색이 의도된 용도). -->
    <Color x:Key="C.Ansi0">#FF24292F</Color>
    <Color x:Key="C.Ansi1">#FFCF222E</Color>
    <Color x:Key="C.Ansi2">#FF116329</Color>
    <Color x:Key="C.Ansi3">#FF7D4E00</Color>
    <Color x:Key="C.Ansi4">#FF0550AE</Color>
    <Color x:Key="C.Ansi5">#FF6639BA</Color>
    <Color x:Key="C.Ansi6">#FF136061</Color>
    <Color x:Key="C.Ansi7">#FF424A53</Color>
    <Color x:Key="C.Ansi8">#FF57606A</Color>
    <Color x:Key="C.Ansi9">#FFA40E26</Color>
    <Color x:Key="C.Ansi10">#FF0A5A22</Color>
    <Color x:Key="C.Ansi11">#FF6B4400</Color>
    <Color x:Key="C.Ansi12">#FF033D8B</Color>
    <Color x:Key="C.Ansi13">#FF512A97</Color>
    <Color x:Key="C.Ansi14">#FF0F5257</Color>
    <Color x:Key="C.Ansi15">#FF24292F</Color>

</ResourceDictionary>
